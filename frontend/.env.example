# Base URL for API calls. Leave empty to use the same origin (works with the Vite dev proxy).
VITE_API_URL=

# Base URL for the WebSocket (Socket.IO) server. Leave empty to use the same origin.
VITE_WS_URL=